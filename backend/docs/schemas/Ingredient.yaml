type: object
properties:
  id:
    type: string
    description: The ID of the ingredient
  source:
    type: string
    enum:
      - Chocolatey
      - Homebrew
      - Homebrew (Cask)
      - App Store
    description: The name of the ingredient source
