export type Item = {
    id           : string,
    name         : string,
    description? : string,
    category?    : string,
    imageUrl?    : string,
    infoUrl?     : string,
    author?      : string,
};
